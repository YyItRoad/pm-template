# DoD [Phase 3] 详细设计(03a + 03b + 03c 共同)

> 全部勾上才允许进 Phase 4。每条对应一份产物。

## 03a 业务流程

- [ ] A.1 每条业务流程的"正常 + 异常"配对齐全(无单独写正常流程)
- [ ] A.2 涉及金额的流程都标注"同事务内联动 balance"(grep 查"同事务"或"事务内"命中 ≥1)
- [ ] A.3 每条流程都有"关联接口"指向 03b

## 03b 接口设计

- [ ] B.1 接口清单 = Phase 2 §3 的 100%(不多不少)
- [ ] B.2 末尾自查:03b 不在 Phase 2 §3 的接口数 = 0
- [ ] B.3 每个接口 4 列必填:鉴权依赖 / 错误码清单 / 幂等策略 / 限频策略(无空白)
- [ ] B.4 入参是"最小集"(每个入参字段在 Phase 1 故事 AC 中能找到出处)
- [ ] B.5 错误码清单覆盖:401 / 403 / 404 / 409 / 422

## 03c 数据表 DDL

- [ ] C.1 DDL 可直接执行(放进 sql/ 跑一遍无语法错误)
- [ ] C.2 字段命名 100% 对齐 charter §3 规范(grep 禁词:admin_user_id 之类的别名)
- [ ] C.3 末尾自查:不在 Phase 2 §2 数据模型里的新表数 = 0

## 通用

- [ ] critic 报告(03a/03b/03c 三份)均无 CRITICAL/HIGH
- [ ] 你本人 review + 签字
