# 模板 [Phase 2] 概要设计

> 用途: 基于 Phase 1 需求,定架构 / 数据模型 / 接口清单 / NFR
> 输入: 来自 Phase 1 需求(每项接口必须能追溯到 S-XX)
> 产出: 概要设计,Phase 3 详细设计的输入
> 禁止: 不写 DDL 详细类型(那是 Phase 3c),不写代码
> 技术选型遵循 [`docs/process/tech_stack.md`](../tech_stack.md) §2-§5 锁级;偏离项须在 `docs/00_charter.md` §2.5 签字。

> **章节必填规则**(本版起强制):
> - `[必填]` 段不可缺,空 = critic CRITICAL
> - `[可选]` 段**两种合法状态**:整段不写 / 写了填内容。**挂了标题但内容是占位符 ___** = `ERR_OPTIONAL_SECTION_FILLED_BUT_BLANK`

## 1. 架构视图 [必填]

> 文字图(模块 / 层级 / 数据流,3-5 段),不写代码、不写 DDL

- 模块/层级:
- 数据流:
- 关键依赖:

## 2. 数据模型(逻辑层) [必填]

> 每张表的目的 / 关键字段 / 唯一约束 / 索引意图

| 表名 | 目的 | 关键字段 | 唯一约束 | 索引意图 |
|---|---|---|---|---|
| ___ | (为什么需要这一张?) | ___ | ___ | ___ |
| ___ |  |  |  |  |

(每张表必须有"为什么需要"的一行注释,防止"先建着以后用")

## 3. 接口清单 [必填]

> **每行必须能追溯到 Phase 1 的 S-XX**,否则"未在需求出现"

| 接口名 | 用途 | 入参主键 | 出参主键 | 鉴权依赖 | 对应故事 |
|---|---|---|---|---|---|
| ___ | ___ | ___ | ___ | ___ | S-XX |

(覆盖 Phase 1 所有 S-ID,无孤儿)

## 4. 非功能需求落点 [必填]

> 4 个子项**必填**,无内容写"N/A"(但不能空)。子项下"为什么这条要落地"必写。

- **性能**: ___
- **安全**: ___
- **可观测**: ___
- **可维护**: ___

## 5. 反向设计(明确不引入,≥3 条) [必填]

> 列 charter §4 禁忌但 AI 容易加上的设计

- ❌ 不引入 ___(常见诱因: ___)
- ❌ 不引入 ___(常见诱因: ___)
- ❌ 不引入 ___(常见诱因: ___)

## 6. 决策记录 [必填]

- YYYY-MM-DD: 砍掉 ___,因为 ___

## 7. 末尾自查:孤儿检查(强制,机械验证) [必填]

```
§3 接口清单中所有 S-ID 不在 Phase 1 故事集合里的接口 = 0 个
```

(可 grep 提取 S-ID 集合,跟 Phase 1 求差集,结果必须空)

## 8. critic + 签字 [必填]

- [ ] critic 自审报告无 CRITICAL/HIGH(报告路径: `docs/process/critics/reports/02_high_level_design_<YYYY-MM-DD>.md`)
- [ ] 你本人 review + 签字: _______ (YYYY-MM-DD)

## 9. 未来扩展 [可选]

> 留 thought experiment 的位置:项目未来 1-2 年可能扩展什么?为它预埋什么 hook?
> **两种合法状态**:
> - 整段不写(简单项目,不需要)
> - 写了要填实际内容(不是占位符)
>
> 写了 `## 9. 未来扩展` 标题但内容是"___"或空 → ERR_OPTIONAL_SECTION_FILLED_BUT_BLANK

- (例)未来可能加 `family` 表做多家庭支持 → 现阶段 user.role 字段加 `family_id` nullable 占位
- (例)未来可能接语音 → API 路径保留 `/api/v1/voice/*` namespace
