# DoD [Phase 3] 详细设计(03a + 03b + 03c 共同)

> 全部勾上才允许进 Phase 4。每条对应一份产物。
> 编号方案:**`D3-NN`**(全局,`D3` = Phase 3,`NN` = 顺序号,从 01 起)。
> 与 critic 引用对齐(critic 写 `D3-NN` 即可定位到此文件具体行)。

## 03a 业务流程

- [ ] **D3-01** §A.1 每条业务流程的"正常 + 异常"配对齐全(无单独写正常流程)
- [ ] **D3-02** §A.2 涉及金额的流程都标注"同事务内联动 balance"(grep 查"同事务"或"事务内"命中 ≥1)
- [ ] **D3-03** §A.3 每条流程都有"关联接口"指向 03b

## 03b 接口设计

- [ ] **D3-04** §B.1 接口清单 = Phase 2 §3 的 100%(不多不少)
- [ ] **D3-05** §B.2 末尾自查:03b 不在 Phase 2 §3 的接口数 = 0
- [ ] **D3-06** §B.3 每个接口 4 列必填:鉴权依赖 / 错误码清单 / 幂等策略 / 限频策略(无空白)
- [ ] **D3-07** §B.4 入参是"最小集"(每个入参字段在 Phase 1 故事 AC 中能找到出处)
- [ ] **D3-08** §B.5 错误码清单覆盖:401 / 403 / 404 / 409 / 422

## 03c 数据表 DDL

- [ ] **D3-09** §C.1 DDL 可直接执行(放进 sql/ 跑一遍无语法错误)
- [ ] **D3-10** §C.2 字段命名 100% 对齐 charter §3 规范(grep 禁词:admin_user_id 之类的别名)
- [ ] **D3-11** §C.3 末尾自查:不在 Phase 2 §2 数据模型里的新表数 = 0

## 通用

- [ ] **D3-12** critic 报告(03a/03b/03c 三份)均无 CRITICAL/HIGH
- [ ] **D3-13** 你本人 review + 签字
- [ ] **D3-14** §A 流程 / §B 接口 / §C DDL 命名、字段类型、接口风格与 `docs/process/tech_stack.md` §3 §4 对齐
